# tiny-was
외부 라이브러리 없이 순수 Java로 구현한 경량 WAS

### 소개
외부 라이브러리 없이 순수 Java만으로 HTTP 요청을 받고 처리하는 경량 WAS입니다.
TCP 소켓, HTTP 파싱, 라우팅, 스레드 풀 관리까지 직접 구현하여 WAS의 내부 동작 원리를 깊이 이해하는 것을 목표로 합니다.

### 기술 스택
- Java 17
- Gradle
- JUnit 5